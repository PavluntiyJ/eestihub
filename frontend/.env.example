NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only override used by docker compose: the frontend container reaches
# the API at http://api:8000, while the browser bundle keeps the public URL.
# API_URL=http://api:8000
